# 全局约束优化总结

审查时间：2026-08-23
审查范围：系统层规则、用户全局规范（`~/.dsh/AGENTS.md`）、工作区规范（`E:\Work Document\AGENTS.md`）、技能库（6 个技能）、本会话运行时策略。

---

## 一、约束来源全景（4 层）

### 第 1 层：系统层（DSH Harness 运行时，不可改）

| 类别 | 规则 |
|---|---|
| 路径 | 工作目录 `E:\Work Document`；DSH 源码 `E:\Deep Seek Harness`；Web GUI `http://127.0.0.1:3080`（改 Web 产物需重启服务 + 强刷才生效） |
| 工具纪律 | read 读文件、write/edit 改文件、glob 找文件、grep 搜内容；不借助 shell 的 cat/find/grep |
| 后台任务 | 跟踪每个 job id；job_output 收集结果、job_kill 停止；子代理默认后台运行 |
| 目标工具 | 长期目标用 goal；workflow / ralph 仅在用户显式要求时使用 |
| 运行时策略 | **动态变化，以每会话快照为准**。本会话：文件策略 danger-full-access、审批策略 never（不得请求 escalation） |

### 第 2 层：用户全局层（`C:\Users\y1954\.dsh\AGENTS.md`，所有工作区生效，项目级不得削弱）

1. **语言**：思考与回复用简体中文；代码、命令、路径、专有名词保留原文。
2. **技能库进化**：`C:\Users\y1954\.agents\skills\<技能名>\SKILL.md`（kebab-case）。任务结束前回顾提取可复用流程；同主题高阶技能更新替换（维护 `CHANGELOG.md`）；新增 → 合并 → 升级替换，保持数量精简、职责单一。
3. **临时文件与产物**：`E:\Work Document\Temp Work Document\<任务名>\`，固定子目录 `Temp Document\`（过程文件）+ `OutPut\`（交付物），必要时任务文件夹根放 `README.md`。

### 第 3 层：工作区层（`E:\Work Document\AGENTS.md`，仅可补充全局）

1. 语言、临时文件组织、技能进化：与全局一致（**重复**，建议改为引用）。
2. **服务启动**（工作区特有）：启动器 `E:\Work Document\启动器\start-dsh.cmd`（桌面快捷方式「启动 DeepSeek Harness」），细节见技能 `dsh-web-launcher`。

### 第 4 层：技能层（`C:\Users\y1954\.agents\skills\`，共 6 个）

| 技能 | 类型 | 何时生效 |
|---|---|---|
| task-output-layout | 元技能 | 任何多步骤任务开始时（组织文件夹） |
| skill-library-evolution | 元技能 | 任何多步骤任务结束前（技能沉淀） |
| agent-artifacts | 元技能 | 任何对话任务收尾（归档 GitHub 仓库 Y-46/agent-artifacts，本地副本 `E:\AgentWorkspace\agent-artifacts`） |
| dsh-web-launcher | 领域技能 | 启动/检查 DSH Web 服务时 |
| dsh-web-plugin | 领域技能 | 开发 DSH Web 前端插件时 |
| pwa-android-apk | 领域技能 | 做 PWA/Android APK 时 |

---

## 二、诊断：重复与不一致

| # | 问题 | 位置 | 优化 |
|---|---|---|---|
| 1 | 语言规范重复 | 全局 §1 ↔ 工作区 §1 | 全局为唯一权威；工作区可删或一行引用 |
| 2 | 临时文件规范重复 | 全局 §3 ↔ 工作区 §2 ↔ 技能 task-output-layout | 全局为权威；工作区仅保留差异点（清理条款）；技能保留流程细节 |
| 3 | 技能进化规范重复 | 全局 §2 ↔ 工作区 §3 | 工作区改为一行引用 |
| 4 | "保留 vs 清理"表述不一致 | 全局 §3 说"保留"；工作区 §2 说"清理无用"；task-output-layout 说"清理无价值" | 统一为：**任务结束清理无价值临时文件，有价值过程文档保留** |
| 5 | 收尾归档规则未入 AGENTS.md | 仅存在于 agent-artifacts 技能 | 建议工作区 AGENTS.md 增加一行引用 |
| 6 | 运行时策略未持久化 | 系统层 | 标注为"动态变化，以会话快照为准"即可 |

---

## 三、优化后的统一约束（合并去重版）

> 后续会话按此清单执行；冲突时上层层级优先（系统层 > 用户全局层 > 工作区层 > 技能层），技能正文与 AGENTS.md 不一致时以 AGENTS.md 为准并回写技能。

1. **语言**：思考与回复用简体中文；代码/命令/路径/专有名词保留原文。
2. **任务组织**（每个多步骤任务）：`Temp Work Document\<任务名>\` 下建 `Temp Document\` + `OutPut\`；任务结束清理无价值临时文件、有价值过程文档保留、成果进 `OutPut\`、复杂任务写 `README.md`。
3. **技能进化**（每个多步骤任务结束前）：回顾 → 提取可复用流程 → 沉淀/合并/升级替换到 `C:\Users\y1954\.agents\skills\`；更新替换时维护 `CHANGELOG.md`；保持技能精简。
4. **产物归档**（每个对话任务收尾）：有价值产物归档 `E:\AgentWorkspace\agent-artifacts`（知识入 `knowledge\`、成果入 `projects\<任务名>\`），commit message 中文；推送失败先本地 commit，下次补推；不收录私密凭据。
5. **服务启动**（本工作区）：DSH Web 用启动器 `E:\Work Document\启动器\start-dsh.cmd` 或桌面快捷方式。
6. **系统层纪律**：工具选择（read/write/edit/glob/grep）、后台任务跟踪（job_output/job_kill）、goal 用于长期目标、workflow/ralph 仅显式要求时用、Web 改动需重启服务+强刷。
7. **运行时策略**：每会话先读快照（文件策略/审批策略），按快照决定可执行操作；approval never 时不得请求 escalation。

---

## 四、落地建议（待确认后执行）

1. `E:\Work Document\AGENTS.md` 精简：§1/§3 改引用全局；§2 保留清理条款；新增 agent-artifacts 一行引用。
2. `~/.dsh/AGENTS.md` §3 补"任务结束清理无价值临时文件"统一表述。
3. 新增技能 `constraints-audit` 固化本次审查流程（本次会话已执行）。

---

*本次审查过程草稿见 `Temp Document\约束来源分析.md`。*
